{
  "name": "@syltra/mqtt-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared MQTT topic namespace and wire-format types between SYLTRA Cloud and the SYLTRA Edge Agent (section 43). Single source of truth so both sides can't silently drift.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "ESLINT_USE_FLAT_CONFIG=false eslint \"src/**/*.ts\" --max-warnings=0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.16.0",
    "@typescript-eslint/parser": "^7.16.0",
    "eslint": "^8.57.0",
    "typescript": "^5.5.3"
  }
}
